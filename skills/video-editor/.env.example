# ElevenLabs API key — https://elevenlabs.io/app/settings/api-keys
# Used for Scribe transcription, which returns WORD-LEVEL timestamps.
# That word-level granularity is the whole point: it's what lets the skill
# place graphics on exact phrases and cut filler precisely.
ELEVENLABS_API_KEY=your_key_here
